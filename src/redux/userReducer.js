/* selectors */
export const getUsers = ({users}) => users;

/* reducer */
export const reducer = (statePart = [], action = {}) => {
    switch (action.type) {
      default:
        return statePart;
    }
  };